import React from 'react';
import './styles.css'

const Header = () => {
    return (
        <div>
            <h1 className="app-title">Upload-Download App</h1>
        </div>
    );
};

export default Header;